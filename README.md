# Auto Test Framework

## 项目简介
这是一个基于Python + Playwright + Pytest的自动化测试框架，支持UI和API测试。

## 快速开始

### 环境准备
```bash
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
playwright install chromium